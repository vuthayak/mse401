# Ranges rather than exact pins: Render builds on Python 3.11 while local
# machines are on 3.13/3.14, and the pinned pydantic wheels do not span both.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
httpx>=0.28,<1.0
pydantic>=2.10,<3.0
google-genai>=1.0,<2.0
python-dotenv>=1.0,<2.0

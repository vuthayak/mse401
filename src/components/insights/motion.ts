/**
 * Re-export shared motion presets so existing insights imports keep working.
 */
export {
  SPRING,
  SPRING_BOUNCE,
  STAGGER_CONTAINER,
  STAGGER_ITEM,
  usePrefersReducedMotion,
} from '../../lib/motion';
